import sys
import math
# Auto-generated code below aims at helping you parse
# the standard input according to the problem statement.

n = int(input())  # Number of elements which make up the association table.
q = int(input())  # Number Q of file names to be analyzed.
ht={}

for i in range(n):
    # ext: file extension
    # mt: MIME type.
    ext, mt = input().split()
    ht[ext]=mt

for i in range(q):
    fname=input().split(".")
    if len(fname) == 1:
        print("UNKNOWN")
    elif len(fname) > 2:
        if "" in fname: 
            fname.remove("")
            fname_ext=fname[1]    
            if fname_ext.upper() in list(ht.keys()) or fname_ext in list(ht.keys()):
                print(ht[fname_ext.lower()])
            else :
                print("UNKNOWN")    
        else:
            print("UNKNOWN")         
    else :
        fname_ext=fname[1]    
        if fname_ext.upper() in [i.upper() for i in list(ht.keys())] or fname_ext in [i.upper() for i in list(ht.keys())]:
            print(ht[fname_ext])
        else :
            print("UNKNOWN")
